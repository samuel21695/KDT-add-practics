function callbackOne(a: number, b: number, callback: (a: number, b: number) => void) {
  callback(a, b);
}

// 함수를 먼저 호출하는 5행이 가장 먼저 실행된다.
callbackOne(5, 3, (a, b) => {
  let result = a + b;
  console.log("덧셈 결과: ", result);
});
